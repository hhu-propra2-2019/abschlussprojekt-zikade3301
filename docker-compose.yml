version: "3.1"
services:
  database:
    image: postgres
    volumes:
      - database-data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=zikade3301
      - POSTGRES_DB=Modulhandbuch
  module:
    build:
      context: .
    depends_on:
      - database
    ports:
      - "8080:8080"

volumes:
  database-data: